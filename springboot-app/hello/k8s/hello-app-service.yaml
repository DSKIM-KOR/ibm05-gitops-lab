apiVersion: v1
kind: Service
metadata:
  name: hello-app-svc
  namespace: hello-app
spec:
# pod와 클라이언트를 연결하기 위해서 서비스가 Nodeport를 이용해서 외부와 연결
  type: NodePort
  selector:
    app: hello-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
